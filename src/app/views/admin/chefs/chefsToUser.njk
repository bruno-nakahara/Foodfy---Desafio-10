{% extends "admin.njk" %}

{% block content %}

    <div class="container-create">
        <div class="title-admin">
            <h2>Chefs</h2>
        </div>
    </div>

    <div class="cards-chefs">

        {% for chef in chefs %}
            <div class="card-chefs">
                <div class="show-chef">
                    <img src="{{chef.src}}" alt="Image of {{chef.name}}" class="img-chef">
                    <div class="chef-name">
                        <h2>{{chef.name}}</h2>
                        
                    </div>
                </div>
            </div>
        {% endfor %}

    </div>

{% endblock %}